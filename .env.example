# JavManager test-only environment variables
#
# - Project configuration should live in `JavManager/appsettings.json` (and optionally `appsettings.Development.json`)
# - Environment variables are only for temporary testing overrides.
#
# Copy this file to `.env.dev` (gitignored) and fill in the values you need.
#
# NOTE:
# `JAVMANAGER_` prefix and `__` nesting are used by the app's config loader.

# =========================
# Remote test environments (used by scripts/remote-test.sh)
# =========================
JAVMANAGER_TEST_WINDOWS_SSH=win@192.168.31.2
JAVMANAGER_TEST_WINDOWS_PASS=
JAVMANAGER_TEST_MACOS_SSH=mac@192.168.31.3
JAVMANAGER_TEST_MACOS_PASS=

# =========================
# JavDB test overrides (optional)
# =========================
JAVMANAGER_JavDb__UserAgent=
